# The dev environment (PATH, linters) is defined once in mise.toml. This file
# just hands it to direnv, so direnv users get the same thing as people running
# `mise activate` — without a second copy of the PATH list to keep in sync.
#
#   direnv allow    # once, after cloning
eval "$(mise env -s bash)"
